{
  "name": "<%= name %>",
  "version": "<%= props.version %>",
  "dependencies": {
  },
  "devDependencies": {
    "angular<%= props.angularUnstable ? '-unstable' : '' %>": "latest",
    "angular-mocks": "latest",
    "jquery": "latest"
  }
}
